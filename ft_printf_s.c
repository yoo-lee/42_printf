#include<stdio.h>

int ft_printf_s(va_list *ap, size_t write_len)
{
	char *str;
	size_t str_len;
	
	str = va_arg(*ap, char *);
	if (str == NULL)
		ft_putstr()
	ft_putchar(va_arg(*ap, int))
	return
}

printf(%d, %s, %c, "you", "niou");
